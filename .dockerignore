node_modules
.nuxt
.output
dist
.git
.github
.idea
.DS_Store
*.log
.env
.env.*
!.env.example

# Fichier genere par pnpm 10/11 en local (champ `allowBuilds`). Il ne declare
# pas de champ `packages:`, donc le pnpm 9 utilise dans l'image le rejette avec
# « ERROR packages field missing or empty ». Inutile au build Docker.
pnpm-workspace.yaml
