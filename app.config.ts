export default defineAppConfig({
  // base = microservice deliveries (3003) · auth = microservice auth (3001)
  api: { base: "https://api.d-livz.com", auth: "https://auth.d-livz.com", },
  // api: { base: "http://localhost:3003", auth: "http://localhost:3001", },
});